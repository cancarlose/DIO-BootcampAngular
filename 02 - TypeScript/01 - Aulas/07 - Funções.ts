function addNumber(x:number, y:number):number {
  return x + y;
}

function addToHello(name:string) {
  return `hello ${name}`
}

let soma: number = addNumber(4, 7);
console.log(addToHello("Carlos"))

// Mudanças do JS: Tipagem.